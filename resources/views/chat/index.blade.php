@extends('layouts.app') @section('title','Pesan') @section('heading','Pesan') @section('subheading','Percakapan dengan pelapor dan penemu.') @section('content')
<div class="panel chat-layout"><div class="chat-list"><h3 style="margin:8px">Percakapan</h3>@forelse($conversations as $conversation)@php($other=$conversation->users->firstWhere('id','!=',auth()->id()))<a class="chat-link" href="{{route('chat.show',$conversation)}}"><img class="avatar" src="{{$other?->avatar ?: 'https://ui-avatars.com/api/?name='.urlencode($other?->name ?? 'User')}}"><div><strong>{{$other?->name}}</strong><div class="muted" style="font-size:12px">{{$conversation->messages->first()?->body ?? 'Mulai percakapan'}}</div></div></a>@empty<div class="empty">Belum ada percakapan.</div>@endforelse</div><div class="empty">Pilih percakapan untuk mulai mengirim pesan.</div></div>
@endsection
