@extends('layouts.app') @section('title','Notifikasi') @section('heading','Notifikasi') @section('subheading','Pembaruan pesan, kecocokan, dan laporan.') @section('content')
<div class="section-head"><div><h2>Aktivitas terbaru</h2></div><form method="post" action="{{route('notifications.read')}}">@csrf<button class="btn btn-soft">Tandai semua dibaca</button></form></div><div class="list">@forelse($notifications as $notification)<div class="panel list-row"><div><strong>{{$notification->data['title'] ?? 'Notifikasi baru'}}</strong><div class="muted">{{$notification->data['message'] ?? 'Ada pembaruan baru untuk Anda.'}}</div></div><small class="muted">{{$notification->created_at->diffForHumans()}}</small></div>@empty<div class="panel empty">Belum ada notifikasi baru.</div>@endforelse</div>
@endsection
