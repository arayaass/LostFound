<!doctype html><html lang="id"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Masuk · TemuKembali</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body><main class="auth-wrap"><div class="auth-card"><x-brand/><h1>Selamat datang kembali</h1><p class="muted">Masuk untuk melanjutkan membantu komunitas.</p>@if(session('auth_error'))<div class="errors">{{session('auth_error')}}</div>@endif @if($errors->any())<div class="errors">{{$errors->first()}}</div>@endif
<a class="btn btn-white google-btn" style="width:100%" href="{{route('google.redirect')}}">G&nbsp; Lanjutkan dengan Google</a><div class="divider">atau dengan email</div><form method="post" action="{{route('login.store')}}">@csrf<div class="field"><label>Email</label><input class="input" type="email" name="email" value="{{old('email')}}" required></div><div class="field"><label for="login-password">Kata sandi</label><div class="password-control"><input id="login-password" class="input" type="password" name="password" autocomplete="current-password" required><button type="button" data-password-toggle aria-controls="login-password" aria-label="Tampilkan kata sandi">Lihat</button></div></div><label class="muted" style="font-size:13px"><input type="checkbox" name="remember"> Ingat saya</label><button class="btn btn-primary" style="width:100%;margin-top:18px">Masuk</button></form><p class="muted">Belum punya akun? <a style="color:var(--blue);font-weight:700" href="{{route('register')}}">Daftar sekarang</a></p></div></main></body></html>
